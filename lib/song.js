const Note = require('./note');
const Util = require('./util');

new Note({
  pos: [200, 25*i],
  vel: [0, 2],
  color: Util.randomColor(),
  game: this,
  id: i
});

const Song = {

};

module.exports = Song;
